---
title: Tenniarb
description: Редактор мозговых карт для MacOS
sidebar:
  # Установить порядок для ссылки (чем ниже число, тем выше будет отображаться ссылка)
  order: 3
  label: Общее
---

import { Icon } from 'astro-icon/components';
import { Image } from 'astro:assets';

import imgGeneralInfo from "../../../../assets/tenniarb/general_info.png";

[Tenniarb](https://github.com/haiodo/tenniarb) – Редактор мозговых карт для MacOS.

#  Tenniarb – Общая информация

Tenniarb – это инструмент для создания и моделирования диаграмм, способный выполнять как простые, так и сложные схемы, легко организовывать их в категории. Создавайте и стилизуйте диаграммы, а встроенный мощный движок JavaScript делает оформление и моделирование действительно эффективным решением для любого быстрого прототипирования.

Каждый элемент диаграммы – объект с набором свойств; некоторые свойства влияют на отображение элемента. Свойства могут быть пользовательскими данными, зависеть от вычислений и других элементов.

В целом каждый элемент диаграммы можно рассматривать как ячейку электронного таблицы, но вместо одной формулы можно определить любой набор правил и свойств.


## Первый шаг

Tenniarb – приложение с несколькими окнами; каждое окно управляет своим файлом в файловой системе. Каждый файл имеет простую текстовую структуру.

Простой контент диаграммы выглядит так:

```
element "Simple" {
    item "Central" {
        font-size 30
        marker "🎁"
    }
}
```

Где `element` описывает логическую структуру слоёв диаграммы, вложенных друг в друга, а `item` – отдельные элементы. Синтаксис прост и удобен для чтения и ручного написания, подробно описан в разделе «Синтаксис». Язык управления содержимым всех элементов называется Tenn. Он основан на известном и старом Tool Command Language (TCL) и имеет чистый, простой в использовании синтаксис.

### Главное окно

Главное окно:

<Image src={imgGeneralInfo} alt="General information" inferSize quality="max" />


Главное окно состоит из следующих структурных компонентов:

* Логическая структура документа:

    Позволяет организовывать диаграммы и управлять их логической структурой с помощью простых операций drag & drop, дублирования и переименования.

* Диаграмма с элементами.
    Поддержка упорядочивания элементов с ссылками, стилей для визуального представления идей, структур и любой необходимой информации.

* Панель свойств.

    Текстовое представление выбранной диаграммы, элемента и управляемых свойствами через язык Tenn.
